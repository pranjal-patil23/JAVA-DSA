class Solution {
    public boolean isPalindrome(int x) {
        if(x<0)
        return false;
        int orig=x;
        int rev=0;
        while(orig!=0){
            rev=rev*10+orig%10;
            orig=orig/10;
            
        }
        return x==rev ;
    }
}
